## Plugin policy

NEVER install or recommend plugins that include MCP servers, LSP servers, or hooks.
Only install plugins that contain skills and/or agents exclusively.
MCP servers, LSP servers, and hooks must be configured manually via ~/.claude/.mcp.json, ~/.claude/.lsp.json, and ~/.claude/settings.json respectively.
